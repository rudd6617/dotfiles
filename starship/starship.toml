format = """
$directory\
$git_branch\
$git_status\
$python\
$cmd_duration\
$line_break\
$character"""

[directory]
truncation_length = 3
truncate_to_repo = true

[git_branch]
format = "[$branch]($style) "

[git_status]
format = '([$all_status$ahead_behind]($style) )'

[cmd_duration]
min_time = 3_000
format = "[$duration]($style) "

[nodejs]
disabled = true

[character]
success_symbol = "[❯](green)"
error_symbol = "[❯](red)"
